# dsh-suggest-prompt 使用说明

> 面向终端用户的操作指南。插件简介、技术原理与配置字段见根目录 [README.md](README.md)。

## 它能做什么

在 DeepSeek Harness 的 Web 界面里，当 AI 完成一轮回答后，插件会用一次**轻量的辅助模型调用**，预测你下一句最可能想说的话，并把这句话以**浅色幽灵文字**显示在输入框内部。

你什么都不用打——看到幽灵文字，直接按 **Tab**，它就会变成输入框里的真实文本，你可以继续编辑或直接回车发送。

效果类似 Claude Code 的"建议下一条提示词"。

```
[你] 帮我修复登录页的 bug
[AI] （修复完成，输出改动说明）……
[输入框] 运行测试验证一下        ← 浅色幽灵文字
          └── 按 Tab → 变成真实输入，可编辑后发送
```

## 安装（三步）

### 第 1 步：把插件加入你的 dsh 部署

当前推荐从源码接入。把本仓库的两个包放进 harness 工作区（或用 `file:` 依赖引用），然后在 profile 补丁层挂两行，例如 `~/.dsh/profiles/web/cordis.patch.yml`：

```yaml
- id: suggest-prompt
  name: '@studyzy/dsh-suggest-prompt'
  config:
    maxInputBytes: 4096
    maxOutputTokens: 512
    timeoutMs: 60000
    maxRecentTurns: 1
    maxTranscriptChars: 12000
    maxSuggestionChars: 240
    acceptKey: Tab

- id: ui-suggest-prompt
  name: '@studyzy/dsh-client-ui-suggest-prompt'
```

> 若你的 dsh 环境已能解析 npm 上的 `@studyzy/*` 包，也可改用 `dsh plugin --profile web add @studyzy/dsh-suggest-prompt` 之类的方式安装。详见 [README.md](README.md) 的「安装」一节。

### 第 2 步：重启 web 服务并刷新页面

重启正在运行的 `dsh web` 服务，浏览器硬刷新页面，然后新建或重新打开一个会话。

### 第 3 步：让 AI 答完一轮，观察输入框

随便问一句。AI 回答完成后，输入框内部就会出现浅色幽灵文字（首次可能需要 10–30 秒生成，因为要走一次辅助模型）。

## 日常使用

| 操作 | 效果 |
|---|---|
| AI 答完一轮后，输入框为空 | 自动出现浅色幽灵建议文字 |
| 光标在输入框里，按 **Tab** | 幽灵文字变成真实输入，可编辑后发送 |
| 直接开始打字 | 幽灵文字消失（被你自己的输入取代） |
| 删掉自己打的字，回到空输入框 | 幽灵文字重新出现（**不会**再消耗模型调用） |
| 不想要这条建议 | 忽略即可，正常输入/回车不受影响 |

### 建议什么时候出现

只有同时满足下面三个条件，幽灵文字才显示：

1. 建议对应**最新**完成的那一轮回答（旧回合的建议不会串出来）；
2. AI 当前**空闲**（正在生成时不会显示）；
3. 输入框**为空**。

### 什么时候"没有建议"

这是**正常现象**，不是故障。当模型判断"下一步不明显"、回复被判定为无效（如 "thanks"、"好的" 这类评价性套话、助手口吻、多句/超长文本）时，插件会静默跳过——不显示、不报错、不打扰。

## 快捷键

默认采纳快捷键是 **Tab**，可配置为任意组合键，例如：

- `Tab`
- `Alt+Slash`（即 `Alt+/`）
- `Ctrl+Enter`

修改方式：在宿主插件配置里设置 `acceptKey`（见上面的 `cordis.patch.yml` 示例），随每条建议下发。

快捷键触发规则：

- 只在焦点位于输入框（textarea）内时生效；
- 中文输入法（IME）组合输入期间的按键不会误触发；
- 只有当幽灵文字正在显示时才会拦截 Tab，否则 Tab 保持默认的焦点切换行为。

## 常见场景示例

| 场景 | AI 回答后，插件可能建议 |
|---|---|
| 让 AI 修 bug 并跑测试 | `运行测试验证一下` |
| 让 AI 写了一段代码 | `试一下效果` |
| AI 询问是否继续 | `继续` / `go ahead` |
| 任务完成，有自然后续 | `提交一下` / `push 上去` |
| 修复后需要确认 | `看看改了什么` |

## 常见问题（FAQ）

**Q：为什么回答完了，输入框里什么都没有？**
A：大概率是"本回合无建议"（模型认为下一步不明显或回复被过滤），属正常现象。若怀疑配置问题，请确认：
- 两个插件都已在 profile 中挂载；
- 重启了 web 服务并硬刷新；
- 辅助模型路由可正常访问（可看 dsh 日志里是否有 `suggest-prompt` 相关的 warn）。

**Q：建议出现得太慢？**
A：辅助模型每次回答完成后才发起，首次可能需要 10–30 秒。可在配置里把 `provider`/`model` 指向更快的模型，或降低 `maxOutputTokens`。

**Q：建议内容不对 / 太长 / 乱码？**
A：插件已做脱敏、净化、语义过滤和长度截断（`maxSuggestionChars`）。仍不满意可调小 `maxSuggestionChars`，或换用推理能力更强的建议模型。

**Q：会不会影响主对话速度 / 花很多钱？**
A：不会。建议在事件循环之外、用轻量模型、短超时生成，每个完成回合至多一次辅助请求；主 agent 请求不增加任何 token。

**Q：按 Tab 没反应？**
A：确认焦点确实在输入框 textarea 内（点击输入框再按），且当前有幽灵文字显示。也可检查 `acceptKey` 是否被改成别的键。

## 配置速查

| 字段 | 作用 | 默认 |
|---|---|---|
| `acceptKey` | 采纳快捷键 | `Tab` |
| `maxRecentTurns` | 发给建议模型的最近回合数 | `1`（只取最后一轮） |
| `maxSuggestionChars` | 建议可见字符上限 | 必填 |
| `maxOutputTokens` | 辅助请求输出 token 上限 | 必填（推理模型建议 ≥512） |
| `timeoutMs` | 辅助请求超时（毫秒） | 必填 |
| `provider` / `model` | 建议模型路由；同时省略则继承主请求路由 | 继承 |

完整字段与说明见 [README.md](README.md) 的「配置」一节。
