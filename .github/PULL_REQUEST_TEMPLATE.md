## Motivation

<!-- What problem does this change solve, and why now? -->

## Changes

<!-- Summarize the implementation. Reference the host (`src/`), browser
     (`src/browser/`), config (`cordis.patch.yml`), or docs touched. -->

## Tests

<!-- How was this verified? Run the relevant unit tests and typecheck:
     pnpm test && pnpm typecheck && pnpm build -->

## Breaking changes

<!-- Does this alter the bundle's public surface, the `suggest-prompt`
     config schema, or the `suggestPrompt` session projection? If so, note
     the migration path. If none, write "None". -->
